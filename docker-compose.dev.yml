services:
  api-core:
    ports:
      - "127.0.0.1:${API_PORT:-5000}:5000"

  ai-driver:
    ports:
      - "127.0.0.1:${AI_DRIVER_PORT:-8000}:8000"

  postgres:
    ports:
      - "127.0.0.1:${POSTGRES_PORT:-5432}:5432"
